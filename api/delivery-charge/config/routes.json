{
  "routes": [
    {
      "method": "GET",
      "path": "/delivery-charge",
      "handler": "delivery-charge.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/delivery-charge",
      "handler": "delivery-charge.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/delivery-charge",
      "handler": "delivery-charge.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
